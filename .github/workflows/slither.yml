name: Slither Analysis
on: [push]
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install libsecret
        run: sudo apt-get install libsecret-0-dev
      - uses: crytic/slither-action@v0.1.1
        with:
          node-version: 14
