[profile.default]
via_ir = false # todo
src = 'contracts'
out = 'out'
libs = [
  'node_modules',
  'lib'
]
test = 'test'
solc_version = '0.8.15'
optimizer_runs = 5000000 # todo can we use process.env?
gas_reports = [ '*' ]

[profile.production]
via_ir = true # todo
optimizer_runs = 5000000 # todo can we use process.env?

# See more config options https://github.com/foundry-rs/fo undry/tree/master/config
